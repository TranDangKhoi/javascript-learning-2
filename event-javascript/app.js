// 1. Sự kiện trong javascript là gì ? (Event -> tương tác với DOM bằng chuột, bàn phím, ...)
// 2. Thêm 1 sự kiện: selector.addEventListener("eventName", handler, [options])
// event: click, keydown, keypress, keyup, mouseover, mousemove, mouseleave, load, DOMCntentLoaded
// handler: function để xử lý khi sự kiện được sinh ra
// tìm hiểu về capture, bubbling
